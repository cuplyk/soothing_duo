name: Auto Comment
on: [issues, pull_request]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: wow-actions/auto-comment@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          issuesOpened: |
            Hello @{{ author }}! 🙌
            We’ve received your issue. Please be patient while we look into it.
            🔍 Pro tip: Add logs or screenshots if relevant!

          pullRequestOpened: |
            Thanks @{{ author }} for your contribution! 🎉
            A maintainer will review your PR soon.
            ⚠️ Don’t forget to sign the CLA if required.
